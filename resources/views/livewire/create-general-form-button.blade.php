<div>
    @if($newExamCan)
    <button wire:click="setExamp"  class="btn btn-primary mb-3">
        <i class="bx bx-plus me-1"></i>
        <span class="align-middle">{{ $btnName }}</span>
    </button>
    @endif
    </div>
